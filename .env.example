# Gitea
GITEA_DB_PASSWORD=changeme
GITEA_SECRET_KEY=changeme

# Woodpecker CI (OAuth app from Gitea)
WOODPECKER_GITEA_CLIENT=changeme
WOODPECKER_GITEA_SECRET=changeme
WOODPECKER_AGENT_SECRET=changeme

# Authentik
AUTHENTIK_DB_PASSWORD=changeme
AUTHENTIK_SECRET_KEY=changeme

# Grafana
GRAFANA_ADMIN_PASSWORD=changeme
GRAFANA_OAUTH_SECRET=changeme  # From Authentik OAuth2 Provider

# DefectDojo
DD_DATABASE_PASSWORD=changeme
DD_SECRET_KEY=changeme
DD_API_KEY=changeme  # Generate in DefectDojo admin panel

# CrowdSec
CROWDSEC_API_KEY=changeme  # docker exec crowdsec cscli bouncers add caddy-bouncer

# n8n
N8N_ENCRYPTION_KEY=changeme
N8N_BASIC_AUTH_PASSWORD_HASH=changeme

# Anthropic (Claude API for AI analysis)
ANTHROPIC_API_KEY=sk-ant-changeme
